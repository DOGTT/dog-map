<!--components/post-bubble/post-bubble.wxml-->
<view class="message-bubble">
  <!-- 头像 -->
  <image class="avatar-zone" src="{{avatarUrl}}" mode="aspectFill"></image>

  <!-- 内容气泡 -->
  <view class="bubble">
    <!-- 头部区 -->
    <view class="header">
      <view class="user-info">
        <text class="username">{{username}}</text>
        <text class="date">{{date}}</text>
      </view>
      <view class="tags">
        <block wx:for="{{tags}}" wx:key="index">
          <text class="tag">{{item}}</text>
        </block>
      </view>
    </view>

    <!-- 内容区 -->
    <view class="content">
      <text class="text">{{text}}</text>
      <!-- 轮播图 -->
      <view class="swiper-container">
        <swiper
          class="image-swiper"
          current="{{currentIndex}}"
          bindchange="onSwiperChange"
          autoplay="{{false}}"
          circular="{{false}}" indicator-dots="true" 
        >
          <block wx:for="{{images}}" wx:key="index">
            <swiper-item>
              <image class="swiper-image" src="{{item}}" mode="aspectFill"></image>
            </swiper-item>
          </block>
        </swiper>
        <!-- 角标 -->
        <view class="index-indicator">
          {{currentIndex + 1}}/{{images.length}}
        </view>
      </view>
     
    </view>

    <!-- 尾部互动区 -->
    <view class="footer">
      <button wx:for="{{reactions}}" wx:key="index" bindtap="onReactionTap" data-index="{{index}}">
        {{item}}
      </button>
    </view>
  </view>
</view>